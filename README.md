# 📚 نظام توصيفات البرامج والمقررات - جامعة الطائف

نظام إلكتروني متكامل لإدارة وأرشفة توصيفات البرامج والمقررات الأكاديمية في جميع كليات وأقسام جامعة الطائف.

## 🌟 المميزات

- ✅ رفع توصيفات البرامج والمقررات (PDF & Word)
- ✅ نظام حالات (تحت المراجعة ← مؤكد)
- ✅ بحث متقدم وفلترة حسب الكلية والقسم والحالة
- ✅ لوحة إدارة لتأكيد التوصيفات
- ✅ قائمة الأعضاء الأكثر نشاطاً
- ✅ آخر الأحداث والتحديثات
- ✅ إمكانية الحذف من قبل العضو الرافع
- ✅ تصميم متجاوب مع جميع الأجهزة
- ✅ واجهة عربية احترافية

## 🚀 طريقة الإعداد

### الخطوة 1: إنشاء مشروع Firebase

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. انقر على **"Add project"** أو **"إضافة مشروع"**
3. أدخل اسم المشروع (مثال: `taif-specs-system`)
4. اتبع الخطوات لإنشاء المشروع

### الخطوة 2: تفعيل الخدمات المطلوبة

#### أ) قاعدة البيانات (Realtime Database):
1. من القائمة الجانبية، اختر **Build** > **Realtime Database**
2. انقر على **"Create Database"**
3. اختر الموقع الأقرب لك
4. اختر **"Start in test mode"** مؤقتاً

#### ب) التخزين (Storage):
1. من القائمة الجانبية، اختر **Build** > **Storage**
2. انقر على **"Get Started"**
3. اختر **"Start in test mode"** مؤقتاً

#### ج) المصادقة (Authentication):
1. من القائمة الجانبية، اختر **Build** > **Authentication**
2. انقر على **"Get Started"**
3. من تبويب **"Sign-in method"**، فعّل **"Anonymous"**

### الخطوة 3: الحصول على بيانات الإعداد

1. اذهب إلى **Project Settings** (⚙️ أعلى القائمة الجانبية)
2. انزل لقسم **"Your apps"**
3. انقر على أيقونة الويب **</>**
4. أدخل اسم التطبيق وسجّله
5. انسخ كائن `firebaseConfig`

### الخطوة 4: تحديث ملف الإعدادات

افتح ملف `firebase-config.js` واستبدل القيم:

```javascript
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  databaseURL: "https://YOUR_PROJECT_ID-default-rtdb.firebaseio.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.firebasestorage.app",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

### الخطوة 5: تحديث قواعد الأمان

#### قواعد Realtime Database:
```json
{
  "rules": {
    "specifications": {
      ".read": true,
      "$specId": {
        ".write": "auth != null && (!data.exists() || data.child('uploaderId').val() == auth.uid || root.child('admins').child(auth.uid).exists())"
      }
    },
    "members": {
      ".read": true,
      "$memberId": {
        ".write": "auth != null && $memberId == auth.uid"
      }
    },
    "admins": {
      ".read": "auth != null",
      ".write": false
    }
  }
}
```

#### قواعد Storage:
```
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /specifications/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
```

### الخطوة 6: النشر على Netlify

#### الطريقة الأولى: عبر GitHub

1. ارفع الملفات إلى مستودع GitHub جديد
2. اذهب إلى [Netlify](https://www.netlify.com/)
3. انقر على **"Add new site"** > **"Import an existing project"**
4. اختر **GitHub** واختر المستودع
5. انقر على **"Deploy site"**

#### الطريقة الثانية: رفع مباشر

1. اذهب إلى [Netlify](https://www.netlify.com/)
2. انقر على **"Add new site"** > **"Deploy manually"**
3. اسحب مجلد المشروع وأفلته

## 📁 هيكل الملفات

```
taif-specs-system/
├── index.html          # الصفحة الرئيسية
├── admin.html          # لوحة الإدارة
├── app.js              # التطبيق الرئيسي (React)
├── style.css           # التنسيقات
├── firebase-config.js  # إعدادات Firebase
├── netlify.toml        # إعدادات Netlify
└── README.md           # هذا الملف
```

## 🔐 لوحة الإدارة

- الرابط: `https://your-site.netlify.app/admin`
- كلمة المرور الافتراضية: `admin123`

> ⚠️ **هام**: غيّر كلمة المرور في ملف `admin.html` قبل النشر!

```javascript
// ابحث عن هذا السطر في admin.html
const ADMIN_PASSWORD = 'admin123';
// غيّره إلى كلمة مرور قوية
const ADMIN_PASSWORD = 'your_secure_password';
```

## 📱 الصفحات

| الصفحة | الرابط | الوصف |
|--------|--------|-------|
| الرئيسية | `/` | عرض الإحصائيات وآخر التوصيفات |
| تصفح | `/browse` | تصفح وبحث التوصيفات |
| رفع | `/upload` | رفع توصيف جديد |
| الأحداث | `/activity` | آخر أحداث الرفع |
| المتصدرين | `/leaderboard` | الأكثر نشاطاً |
| الإدارة | `/admin` | لوحة التحكم |

## 🎨 تخصيص الألوان

يمكنك تغيير ألوان الموقع من ملف `style.css`:

```css
:root {
    --primary: #1e3a5f;      /* اللون الأساسي */
    --secondary: #c8a961;    /* اللون الثانوي */
    --success: #10b981;      /* لون النجاح */
    --warning: #f59e0b;      /* لون التحذير */
    --danger: #ef4444;       /* لون الخطر */
}
```

## 🔧 التحديثات المستقبلية

- [ ] إضافة نظام تسجيل دخول للأعضاء
- [ ] إضافة إشعارات البريد الإلكتروني
- [ ] تصدير التقارير بصيغة Excel
- [ ] إضافة نظام التعليقات
- [ ] دعم رفع ملفات متعددة

## 📞 الدعم

للمساعدة أو الاستفسارات:
- 📧 البريد الإلكتروني: [support@example.com]
- 🌐 الموقع: [جامعة الطائف](https://www.tu.edu.sa)

## 📄 الترخيص

هذا المشروع مرخص للاستخدام الأكاديمي في جامعة الطائف.

---

تم التطوير بـ ❤️ لجامعة الطائف
